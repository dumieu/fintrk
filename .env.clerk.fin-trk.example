# Copy to .env.clerk.fin-trk (gitignored) and fill from:
# https://dashboard.clerk.com → Applications → FinTRK → Configure → API keys
#
# Use the same environment on Clerk and Vercel (e.g. Production keys for fintrk.io).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
